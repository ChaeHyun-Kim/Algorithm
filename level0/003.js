// 숫자 비교하기
function solution(num1, num2) {
  return num1 === num2 ? 1 : -1;
}

// 런타임 에러!
// 삼항연산자 중괄호 필요없음!
function solution(num1, num2) {
  // let answer = {num1 === num2 ? 1 : -1};
  return answer;
}
